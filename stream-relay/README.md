# Stream relay (legacy)

This folder was used for the **Mux** browser → RTMP relay (WebSocket + ffmpeg).

Live streaming in the app now uses **LiveKit** (WebRTC in the browser). You can delete this folder if you are not using it.
